services:
     app:
       image: 'jc21/nginx-proxy-manager:latest'
       ports:
         - '80:80'   # HTTP
         - '81:81'   # NPM Dashboard
         - '443:443' # HTTPS
       volumes:
         - ./data:/data
         - ./letsencrypt:/etc/letsencrypt
       restart: always